apiVersion: v1
kind: Service
metadata:
  name: cadprice-api
  namespace: cadprice
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: cadprice
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: cadprice
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
